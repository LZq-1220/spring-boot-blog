# 个人博客系统 — 从零到一完整学习指南

> 这份文档带你从头梳理整个项目的开发流程，理解每一层架构的设计思路，并说明每个技术点**干什么用**、**为什么选它**、**怎么学**。
> 面向零基础，不需要你有任何后端经验，但你需要知道 Java 的基本语法（变量、类、方法、if/for）。

---

## 目录

1. [这个项目是什么](#1-这个项目是什么)
2. [整体架构：分层设计](#2-整体架构分层设计)
3. [技术栈全景图](#3-技术栈全景图)
4. [开发流程：一步步搭建](#4-开发流程一步步搭建)
5. [技术深入讲解](#5-技术深入讲解)
   - [Spring Boot — 骨架](#51-spring-boot--骨架)
   - [Spring Data JPA — 数据库操作](#52-spring-data-jpa--数据库操作)
   - [Spring Security — 认证与授权](#53-spring-security--认证与授权)
   - [JWT — 无状态登录](#54-jwt--无状态登录)
   - [RESTful API 设计](#55-restful-api-设计)
   - [异常处理](#56-异常处理)
   - [环境隔离 (Profile)](#57-环境隔离-profile)
6. [关键设计决策与踩坑记录](#6-关键设计决策与踩坑记录)
7. [可以进一步学习的点](#7-可以进一步学习的点)

---

## 1. 这个项目是什么

一个**前后端分离**的**个人博客系统**的后端部分。

**前后端分离**：后端只负责提供数据（JSON 格式的 API），前端（另一个独立项目）负责展示界面。它们之间通过 HTTP 请求通信。

**核心功能**：
- 用户注册、登录
- 发表文章、编辑文章、删除文章
- 文章按分类/标签筛选
- 文章评论（支持回复，两层嵌套）
- 后台管理（权限控制：普通用户 vs 管理员）

**数据流向示意**：

```
浏览器/前端 → HTTP请求 → 后端 Controller → Service → Repository → 数据库
                                  ↑ 返回 JSON 响应 ↓
```

---

## 2. 整体架构：分层设计

任何一个正规的后端项目，都不会把所有代码写在一个文件里。我们按照**职责**分成若干层，每层只做一件事：

```
┌──────────────────────────────────┐
│          Controller 层           │  ← 接收 HTTP 请求，返回 HTTP 响应
│  (AuthController, ArticleController...)  │     不写业务逻辑，只做"传送带"
└──────────────────┬───────────────┘
                   │ 调用
┌──────────────────▼───────────────┐
│           Service 层             │  ← 核心业务逻辑
│  (AuthService, ArticleService...)│     事务管理、权限校验、数据组装
└──────────────────┬───────────────┘
                   │ 调用
┌──────────────────▼───────────────┐
│         Repository 层            │  ← 和数据库对话
│  (UserRepository, ArticleRepo...)│     增删改查，不写业务逻辑
└──────────────────┬───────────────┘
                   │
┌──────────────────▼───────────────┐
│         数据库 (MySQL / H2)      │
└──────────────────────────────────┘
```

**为什么要分层？**

- 如果你想把数据库从 MySQL 换成 PostgreSQL：只改 Repository 层，Service 和 Controller 完全不用动
- 如果你想给前端加一个手机 App 的接口：复用 Service，只加新的 Controller
- 如果你想写单元测试：可以单独测 Service，mock 掉 Repository

### 各层的具体代码

#### Controller 层

```java
@RestController                     // "我是一个 REST API 控制器"
@RequestMapping("/api/articles")    // "我的所有接口路径都以 /api/articles 开头"
public class ArticleController {

    private final ArticleService articleService;  // 依赖注入

    @GetMapping                     // 处理 GET 请求
    public ResponseEntity<Page<ArticleSummary>> getArticles(...) {
        return ResponseEntity.ok(articleService.getPublishedArticles(...));
        //       ↑ 返回 HTTP 200 + JSON 数据
    }

    @PostMapping                    // 处理 POST 请求
    public ResponseEntity<ArticleResponse> createArticle(
            @Valid @RequestBody ArticleRequest request,  // 从请求体读 JSON，自动校验
            Authentication auth) {                        // Spring Security 注入当前用户
        return ResponseEntity.ok(articleService.createArticle(request, auth.getName()));
    }
}
```

关键点：
- `@RestController` = `@Controller` + 所有方法返回值自动转 JSON
- `@GetMapping` / `@PostMapping` / `@PutMapping` / `@DeleteMapping` 对应 HTTP 的 GET / POST / PUT / DELETE
- `@Valid` 触发参数校验（在 DTO 类上定义的 `@NotBlank` 等注解）
- `Authentication auth` — Spring Security 自动把当前登录用户注入给你，不需要手动从 session 取

#### Service 层

```java
@Service
@RequiredArgsConstructor         // Lombok 自动生成构造函数，省去 @Autowired
public class ArticleService {

    private final ArticleRepository articleRepository;
    private final CategoryRepository categoryRepository;
    // ... 其他依赖

    @Transactional                // 这个方法里的所有数据库操作在一个事务中
    public ArticleResponse createArticle(ArticleRequest request, String username) {
        User author = userRepository.findByUsername(username)
                .orElseThrow(() -> new NotFoundException("用户不存在"));
        //               ↑ Optional 的用法：有值就返回，没值就抛异常

        Article article = Article.builder()    // Builder 模式创建对象
                .title(request.getTitle())
                .content(request.getContent())
                .status(parseStatus(request.getStatus()))
                .author(author)
                .build();

        article = articleRepository.save(article);  // 保存到数据库
        return ArticleResponse.fromEntity(article, 0L);
    }
}
```

关键点：
- `@Transactional` — 事务。如果中间任何一步出错，之前的所有数据库操作自动回滚
- Builder 模式 — 避免了冗长的构造器参数，代码清晰
- `Optional.orElseThrow()` — Java 8 引入，优雅处理"找不到"的情况

#### Repository 层

```java
public interface ArticleRepository extends JpaRepository<Article, Long> {
    // 你只需要定义方法签名，Spring Data JPA 自动实现！
    // 它根据方法名推导出 SQL 语句

    Page<Article> findByStatus(Article.Status status, Pageable pageable);
    // → SELECT * FROM articles WHERE status = ? LIMIT ? OFFSET ?

    // 复杂查询可以手写 JPQL（类似 SQL，但操作的是 Java 对象而非表）
    @Query("SELECT a FROM Article a LEFT JOIN FETCH a.author WHERE a.id = :id")
    Article findByIdWithAll(@Param("id") Long id);

    // 不是查询，是更新操作，需要 @Modifying
    @Modifying
    @Query("UPDATE Article a SET a.viewCount = a.viewCount + 1 WHERE a.id = :id")
    void incrementViewCount(@Param("id") Long id);
}
```

关键点：
- `JpaRepository<Article, Long>` — 泛型参数：实体类型 + 主键类型。继承后自动获得 `save()`、`findById()`、`findAll()`、`delete()` 等基础方法
- 方法名约定：`findBy` + 字段名 → 自动生成 WHERE 查询；`findByCategoryIdAndStatus` → WHERE category_id = ? AND status = ?
- `JPQL` — Java Persistence Query Language。和 SQL 很像，但用的是类名和属性名，不是表名和列名
- `JOIN FETCH` — 后面会详细讲

---

## 3. 技术栈全景图

```
┌───────────────────────────────────────────────────────┐
│                    你的博客项目                         │
├───────────────────────────────────────────────────────┤
│                                                       │
│  运行环境：Java 17 + Maven（构建工具）                   │
│  框架：Spring Boot 3.2.5（核心骨架）                    │
│                                                       │
│  ┌─────────────────────┐  ┌─────────────────────────┐ │
│  │   Web 层             │  │   数据层                  │ │
│  │  Spring Boot Web    │  │  Spring Data JPA        │ │
│  │  (REST API 框架)     │  │  (ORM：对象-关系映射)     │ │
│  │  Jakarta Validation │  │  Hibernate (JPA 实现)    │ │
│  │  (参数校验)          │  │  MySQL / H2 (数据库)     │ │
│  └─────────────────────┘  └─────────────────────────┘ │
│                                                       │
│  ┌─────────────────────┐  ┌─────────────────────────┐ │
│  │   安全层              │  │   工具/辅助               │ │
│  │  Spring Security    │  │  Lombok (减少样板代码)    │ │
│  │  JWT (jjwt 0.12)    │  │  Maven (依赖管理+打包)    │ │
│  │  BCrypt (密码加密)   │  │                         │ │
│  └─────────────────────┘  └─────────────────────────┘ │
│                                                       │
└───────────────────────────────────────────────────────┘
```

---

## 4. 开发流程：一步步搭建

如果你要从零复刻这个项目，下面是真实开发顺序：

### 第一步：初始化项目

用 [Spring Initializr](https://start.spring.io/) 生成项目骨架，或者直接在 IDE 里新建 Spring Boot 项目。你需要加这些依赖：

- Spring Web — 提供 REST API 能力
- Spring Data JPA — 操作数据库
- Spring Security — 认证授权
- Spring Validation — 参数校验
- MySQL Driver — 连接 MySQL
- H2 Database — 内存数据库，开发测试用
- Lombok — 减少 getter/setter/构造器代码
- jjwt (io.jsonwebtoken) — JWT 生成和解析（这个需要手动加，Initializr 里没有）

### 第二步：定义数据模型（Entity）

这是整个项目的地基。在写任何业务代码之前，先搞清楚"我的数据长什么样"。

**问自己三个问题**：
1. 系统里有哪些"东西"？ → 用户(User)、文章(Article)、评论(Comment)、分类(Category)、标签(Tag)
2. 每个"东西"有哪些属性？ → 用户有用户名、密码、邮箱等；文章有标题、内容、状态等
3. 这些"东西"之间什么关系？ → 一个用户可以写多篇文章（一对多）；一篇文章有多个标签（多对多）

然后画出 ER 图（实体关系图）：

```
User ──1────────N──> Article ──1────────N──> Comment
                       │                       │
                       │ N                     │ (自引用 parent_id)
                       │                       │
                   Category                    └──> Comment (回复)

Article ──N────────M──> Tag  (通过中间表 article_tags)
```

对应到 JPA 注解：
- `@OneToMany` / `@ManyToOne` — 一对多 / 多对一
- `@ManyToMany` — 多对多（自动生成中间表）
- `@JoinColumn` — 外键列名

### 第三步：写 Repository

有了 Entity，就可以定义 Repository 接口。先写基本的增删改查，后面需要什么查询再加方法。

### 第四步：写 Service（业务逻辑）

这是项目的大脑。先把核心业务流程想清楚：

- **注册**：查用户名/邮箱是否重复 → 密码加密 → 存用户 → 生成 JWT 返回
- **登录**：验证用户名密码 → 生成 JWT 返回
- **发表文章**：查用户 → 查分类/标签 → 构建文章对象 → 存库
- **查看文章**：查文章 → 加阅读量 → 查评论数 → 组装返回
- **发评论**：查文章 → 查用户 → 查父评论（如果有）→ 存评论

### 第五步：写 Controller

把 Service 暴露成 HTTP 接口。每个接口的路径、HTTP 方法、参数、返回值要想好。

### 第六步：加安全层

- 配 Spring Security：哪些 URL 公开，哪些需要登录，哪些需要管理员
- 写 JWT 工具类：生成 token、解析 token
- 写 JWT 过滤器：拦截每个请求，从 Header 取 token，解析并注入上下文

### 第七步：异常处理

把所有 Service 抛出的异常在 `@RestControllerAdvice` 里统一处理成规范的 JSON 错误响应。

### 第八步：环境配置

区分开发环境（H2 内存数据库，方便快速启动）和生产环境（MySQL，持久化存储）。

---

## 5. 技术深入讲解

### 5.1 Spring Boot — 骨架

**它干了什么？**

Spring Boot 本质上是一个"自动配置器"。传统的 Spring 项目你需要写大量 XML 配置文件来告诉框架"数据库在哪"、"事务怎么管"、"MVC 怎么配"。Spring Boot 的哲学是"约定大于配置"——你加一个依赖，它自动帮你配好。

比如你加了 `spring-boot-starter-web`，它就自动：
- 启动内嵌的 Tomcat 服务器（你不用自己装 Tomcat）
- 注册 `DispatcherServlet`（所有 HTTP 请求的入口）
- 配置 Jackson（Java 对象 ↔ JSON 的自动转换）

你加了 `spring-boot-starter-data-jpa`，它就自动：
- 创建 `DataSource`（数据库连接池）
- 创建 `EntityManagerFactory`（JPA 核心引擎）
- 启用事务管理

**你要学什么？**

- `pom.xml` 里 `<parent>` 标签的作用（继承 Spring Boot 的默认版本管理）
- `@SpringBootApplication` 注解的秘密（它其实是三个注解的合体：`@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`）
- `application.yml` 配置文件的语法和常用配置项
- Starter 机制（`spring-boot-starter-*` 是一组预打包的依赖集合）

### 5.2 Spring Data JPA — 数据库操作

**它干了什么？**

JPA (Java Persistence API) 是一套规范，定义了"Java 对象和数据库表怎么映射"。Hibernate 是这套规范的具体实现。

最简单的理解：**你写 Java 类，Hibernate 自动建表；你调 Java 方法，Hibernate 自动生成 SQL**。

```java
// 你写的 Java 代码
User user = userRepository.findByUsername("zhangsan");

// Hibernate 自动生成的 SQL（你不需要自己写）：
// SELECT * FROM users WHERE username = 'zhangsan';
```

**核心概念：**

| 概念 | 解释 | 注解 |
|------|------|------|
| 实体 Entity | 对应数据库一张表 | `@Entity`, `@Table` |
| 主键 | 唯一标识一行 | `@Id`, `@GeneratedValue` |
| 列映射 | 属性对应字段 | `@Column` |
| 关联关系 | 表之间的外键 | `@OneToMany`, `@ManyToOne`, `@ManyToMany` |
| 懒加载 LAZY | 关联数据不立即查，用到时才查 | `fetch = FetchType.LAZY` |
| 急加载 EAGER | 关联数据和主数据一起查 | `fetch = FetchType.EAGER` |

**N+1 问题（面试高频考点）：**

假设你有 10 篇文章，每篇文章有一个作者。如果你用 LAZY 加载：

```java
List<Article> articles = articleRepository.findAll();  // 1 条 SQL
for (Article a : articles) {
    System.out.println(a.getAuthor().getName());       // 每条触发 1 条 SQL
}
// 总共 1 + 10 = 11 条 SQL，这就是 N+1 问题
```

你的项目里用了两种方法解决：

```java
// 方法一：JOIN FETCH 一条 SQL 把关联数据也查出来
@Query("SELECT a FROM Article a LEFT JOIN FETCH a.author WHERE a.id = :id")

// 方法二：批量查询代替循环查询
@Query("SELECT c.article.id, COUNT(c) FROM Comment c WHERE c.article.id IN :ids GROUP BY c.article.id")
List<Object[]> countByArticleIdIn(@Param("ids") Collection<Long> ids);
```

**你要学什么？**

- JPA 实体注解（`@Entity`, `@Table`, `@Column`, `@Id`, `@GeneratedValue` 等）
- 关联关系映射（`@OneToMany`, `@ManyToOne`, `@ManyToMany`）
- Fetch 策略（LAZY vs EAGER，各自优缺点）
- Spring Data JPA 方法命名规则
- JPQL 基本语法
- 事务：`@Transactional` 的含义、传播机制、回滚条件
- `open-in-view` 是什么，为什么 REST API 要关掉它

### 5.3 Spring Security — 认证与授权

**它干了什么？**

Spring Security 是一个"过滤器链"（Filter Chain）。每个 HTTP 请求到达 Controller 之前，要先经过一连串的过滤器。这些过滤器负责：
1. **认证 (Authentication)**：你是谁？（验证用户名密码 / 解析 JWT）
2. **授权 (Authorization)**：你能干什么？（普通用户 / 管理员权限）

**你项目里的配置拆解：**

```java
http
    .cors(cors -> ...)           // 允许前端跨域访问（前端和后端不同端口）
    .csrf(csrf -> csrf.disable()) // 关闭 CSRF 防护（前后端分离用 JWT，不需要）
    .sessionManagement(session ->
        session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
        // ↑ 关键！不创建 Session，每个请求都从 JWT 解析身份，实现无状态
    .authorizeHttpRequests(auth -> auth
        .requestMatchers("/api/auth/**").permitAll()           // 注册登录公开
        .requestMatchers("/api/articles/my/**").authenticated() // 我的文章需登录
        .requestMatchers(HttpMethod.GET, "/api/articles/**").permitAll() // 看文章公开
        .anyRequest().authenticated()                          // 其他都要登录
    )
    .addFilterBefore(jwtAuthFilter, UsernamePasswordAuthenticationFilter.class);
    // ↑ 把自己的 JWT 过滤器插在 Spring Security 默认认证过滤器之前
```

**为什么匹配顺序很重要？**

Spring Security 的匹配器是**从上到下**匹配的，第一条匹配就生效。你把 `/api/articles/my/**` 写在 `GET /api/articles/**` 上面，是因为"/my"路径也被第二条 `GET /api/articles/**` 覆盖。如果顺序反了，`/my` 会先匹配到 `permitAll`，导致没有认证信息。

**你要学什么？**

- 认证 vs 授权的区别
- Filter Chain 的概念（一个请求经过哪些过滤器）
- `SecurityFilterChain` 的配置方法
- `AuthenticationManager` 和 `PasswordEncoder` 的作用
- CSRF 是什么，为什么 REST API 通常关闭它
- CORS 是什么，为什么前后端分离需要配置它
- Session 和 JWT 两种认证方式的区别（有状态 vs 无状态）

### 5.4 JWT — 无状态登录

**它干了什么？**

JWT (JSON Web Token) 本质上是一张"电子身份证"。

```
流程：
1. 用户登录 → 服务器验证密码 → 返回一个 token（加密的字符串）
2. 前端存下 token → 后续每个请求都在 Header 里带 Authorization: Bearer <token>
3. 服务器 JWT 过滤器拦截请求 → 从 Header 取 token → 解密验证 → 拿到用户信息 → 放行
```

**JWT 的结构：**

```
eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJ6aGFuZ3NhbiJ9.签名部分
│                    │                      │
Header              Payload                Signature
(算法类型)         (用户数据 claims)       (防篡改签名)
```

- Header：声明签名算法（你用的是 HMAC-SHA512）
- Payload：存放你想放的数据——你的代码放了 userId、username、role
- Signature：用密钥对前两部分签名，确保没人能篡改

**你代码中的关键细节：**

```java
public String generateToken(User user) {
    return Jwts.builder()
            .subject(user.getUsername())           // subject = 用户名
            .claim("userId", user.getId())         // 自定义字段
            .claim("role", user.getRole().name())
            .issuedAt(new Date())                  // 签发时间
            .expiration(new Date(now + 86400000))  // 过期时间（24小时）
            .signWith(key)                         // 签名
            .compact();
}
```

**为什么用 JWT 而不用传统 Session？**

| | Session (有状态) | JWT (无状态) |
|---|---|---|
| 服务器存储 | 需要存储 Session，内存/Redis | 不需要存，token 自包含信息 |
| 多服务器扩展 | 需要共享 Session（Redis 等） | 天然支持，任何服务器都能验证 |
| 注销 | 直接删 Session | 需要额外机制（黑名单等） |
| 适用场景 | 单体应用 | 分布式系统、微服务、前后端分离 |

**你要学什么？**

- JWT 的三段式结构
- Claims（声明）的概念
- 签名算法（对称 HMAC vs 非对称 RSA）
- token 过期策略和刷新机制（Access Token + Refresh Token）
- JWT 的安全性注意事项（密钥管理、不在 payload 放敏感信息——因为只是 Base64 编码，不是加密）

### 5.5 RESTful API 设计

REST 是一种 API 设计风格，不是硬性规范。核心思想是**用 URL 定位资源，用 HTTP 方法表达操作**。

**你的项目中的 REST 设计：**

| HTTP 方法 | URL | 含义 |
|-----------|-----|------|
| POST | `/api/auth/register` | 注册 |
| POST | `/api/auth/login` | 登录 |
| GET | `/api/articles?page=0&size=10` | 文章列表 |
| GET | `/api/articles/{id}` | 文章详情 |
| POST | `/api/articles` | 创建文章 |
| PUT | `/api/articles/{id}` | 更新文章 |
| DELETE | `/api/articles/{id}` | 删除文章 |
| GET | `/api/articles/{id}/comments` | 文章评论列表 |
| POST | `/api/articles/{id}/comments` | 发表评论 |
| DELETE | `/api/articles/{id}/comments/{id}` | 删除评论 |

**设计原则：**
- URL 里用名词（资源），不用动词。比如 `/api/articles` 而不是 `/api/getArticles`
- 用 HTTP 方法表达动作：GET 查、POST 增、PUT 改、DELETE 删
- 层级关系用路径体现：`/articles/{id}/comments` 表示某篇文章下的评论
- 分页/筛选用查询参数：`?page=0&size=10&categoryId=1`

### 5.6 异常处理

你的项目做了一个很专业的全局异常处理：

```java
@RestControllerAdvice   // = @ControllerAdvice + @ResponseBody
                        // 拦截所有 Controller 抛出的异常，返回统一 JSON
public class GlobalExceptionHandler {

    @ExceptionHandler(ApiException.class)          // 自定义业务异常 → 400
    @ExceptionHandler(MethodArgumentNotValidException.class) // 参数校验失败 → 400
    @ExceptionHandler(AuthenticationException.class)  // 密码错误 → 401
    @ExceptionHandler(AccessDeniedException.class)     // 权限不足 → 403
    @ExceptionHandler(Exception.class)                 // 兜底 → 500
}
```

**为什么需要这个？** 如果没有全局异常处理，任何没 catch 的异常都会变成 Tomcat 默认的 500 错误页面（HTML，丑且前端无法解析）。统一处理后，所有错误都有结构化的 JSON 响应：`{"message": "..."}`，前端可以统一处理。

**你的代码还特别注意了**：token 过期返 401 并提示"请重新登录"，而权限不足返 403 并提示"没有权限"。前端可以根据状态码决定是跳登录页还是只弹个提示。

### 5.7 环境隔离 (Profile)

你的 `application.yml` 里用 `---` 分隔了多段配置：

```yaml
spring:
  profiles:
    active: h2       # 当前用 h2 环境

---
spring:
  config:
    activate:
      on-profile: h2   # 这段配置只在 h2 环境生效
  datasource:
    url: jdbc:h2:mem:blog         # 内存数据库，重启即消失

---
spring:
  config:
    activate:
      on-profile: mysql   # 这段配置只在 mysql 环境生效
  datasource:
    url: jdbc:mysql://localhost:3306/blog  # 真实 MySQL
```

**好处**：开发时用 H2，启动快、不依赖外部 MySQL；部署时切换到 MySQL，数据持久化。切换只需要改一行 `active: mysql`。

---

## 6. 关键设计决策与踩坑记录

你在开发这个项目时沉淀的几个工程决策，每一个都值得仔细理解：

### 6.1 关闭 open-in-view

```yaml
spring.jpa.open-in-view: false
```

`open-in-view=true` 时，Hibernate Session 会一直保持到 HTTP 响应写完才关闭。在前后端分离的 REST API 里，响应写给前端之后什么都不会发生，白白占着数据库连接。更关键的是，关了之后你被迫在 Service 层就完成所有数据加载（否则访问 LAZY 属性会抛异常），这反而让代码更可控、更清晰。

### 6.2 不用 cascade=ALL + orphanRemoval 删评论

Comment 表有自引用外键 `parent_id`。如果让 Hibernate 的级联删除自动处理，它会按**任意顺序**删子行，可能先删父评论撞外键约束。所以你在 Service 里手动控制顺序：先删回复 → 删根评论 → 删文章。

### 6.3 viewCount 用数据库原子 UPDATE 而非读改写

```java
// 错误做法（并发下有丢失更新问题）：
article.setViewCount(article.getViewCount() + 1);
articleRepository.save(article);

// 正确做法（数据库原子操作）：
articleRepository.incrementViewCount(id);  // UPDATE articles SET view_count = view_count + 1 WHERE id = ?
```

如果两个请求同时读到 viewCount=100，各加一，各自存 101——结果就丢了 1 次计数。SQL 的 `SET x = x + 1` 是数据库层面的原子操作，不会丢。

### 6.4 标签集合用 clear() + addAll() 而非 setTags(new HashSet<>())

```java
// 正确
article.getTags().clear();
article.getTags().addAll(newTags);

// 有问题
article.setTags(new HashSet<>(newTags));
```

因为 Hibernate 对 `@ManyToMany` 关联的集合使用了代理对象（PersistentSet），直接替换集合引用会让 Hibernate 丢失对该集合的跟踪，可能导致脏检查失败或产生异常数据。

### 6.5 DTO 和 Entity 分离

你的项目里 Entity 是数据库的映射，DTO 是给前端看的。两者分离的好处：
- 数据库表变了（比如加个 `deleted_at` 字段），不影响前端已有的接口
- 列表接口可以不返回文章正文（`ArticleSummary` 没 content 字段），节省带宽
- 敏感字段（如密码）永远不会通过 DTO 暴露出去

### 6.6 评论只支持两层

这是一个工程决策：技术上可以无限嵌套（不断关联 parent_id），但你的接口设计只返回两层。所以发评论时做了判断——如果回复对象本身就是一条回复，就把它挂到根评论下。这保证了数据结构不会超出接口能表达的深度，避免了数据存进去但前端永远查不出来的情况。

---

## 7. 可以进一步学习的点

当你吃透当前项目后，可以顺着这些方向继续深入学习：

### 短期（对当前项目的改进）

1. **注册并发问题**：`existsByUsername` 检查 + `save` 不是原子操作。两个请求同时注册同一个用户名可能都通过检查。数据库唯一约束是最后一道防线，但你需要 capture `DataIntegrityViolationException`。
2. **评论软删除**：`Comment.visible` 字段已经建好了，但删除接口现在是硬删除。可以改为软删除（`visible = false`），保留评论记录。
3. **单元测试**：你的 Service 层很适合写单元测试（依赖都通过构造器注入，可以 mock）
4. **API 文档**：用 SpringDoc (Swagger) 自动生成交互式 API 文档

### 中期（技术广度）

5. **Redis 缓存**：文章列表、热门文章可以缓存到 Redis，减少数据库压力
6. **图片上传**：文章的富文本内容如果支持图片，需要学文件上传、OSS（阿里云/腾讯云对象存储）
7. **日志系统**：目前只有简单的 log，生产环境需要接入 ELK（Elasticsearch + Logstash + Kibana）
8. **MyBatis**：你的简历里写了"了解 MyBatis"。JPA 是 ORM（自动生成 SQL），MyBatis 是 SQL Mapper（你写 SQL，它帮你映射结果）。两者各有适用场景，都掌握最好

### 长期（架构/底层）

9. **Spring 核心原理**：IoC 容器、Bean 生命周期、AOP（面向切面编程）
10. **JVM 基础**：内存模型、垃圾回收、类加载机制
11. **并发编程**：线程池、synchronized、Lock、volatile、CAS
12. **分布式系统**：微服务（Spring Cloud）、消息队列（RabbitMQ/Kafka）、分布式事务

---

> **建议的学习路线**：先把当前项目的每一行代码都读懂 → 尝试加一个新功能（比如点赞/收藏）→ 给项目写单元测试 → 学习 Redis 并接入缓存 → 深入 Spring 源码。
>
> 不要贪多嚼不烂。你现在做的这个博客项目，已经覆盖了一个后端开发 70% 的日常工作内容。把它吃透，面试够用。
